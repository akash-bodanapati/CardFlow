# Define tools for the LangGraph agent if using ToolNode
# For this project, we can just use regular node functions calling the services directly
# since we have a defined explicit workflow graph rather than an LLM agentic tool-calling loop.
